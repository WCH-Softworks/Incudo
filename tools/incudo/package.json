{
  "name": "@incudo/cli",
  "version": "0.0.1",
  "private": true,
  "description": "incudo — import, validate and inspect content without a UI. The engine's first consumer.",
  "license": "MIT",
  "type": "module",
  "bin": { "incudo": "./src/cli.ts" },
  "scripts": { "build": "tsc --build" },
  "dependencies": {
    "@incudo/core": "*",
    "@incudo/content": "*",
    "@incudo/aurora-import": "*"
  }
}
