{
  "name": "@heroforge/cli",
  "version": "0.0.1",
  "private": true,
  "description": "hf — import, validate and inspect content without a UI. The engine's first consumer.",
  "license": "MIT",
  "type": "module",
  "bin": { "hf": "./src/cli.ts" },
  "scripts": { "build": "tsc --build" },
  "dependencies": {
    "@heroforge/core": "*",
    "@heroforge/content": "*",
    "@heroforge/aurora-import": "*"
  }
}
