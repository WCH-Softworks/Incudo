{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Incudo window is allowed to do. See apps/desktop/README.md for why the HTTP scope is as wide as it is.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [{ "url": "https://**" }],
      "deny": [{ "url": "http://**" }]
    }
  ]
}
